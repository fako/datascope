# Should go over all modules in HIF.input.http
# And find all QueryLink classes
# Then it should run a test that tests all these classes.
# Whether results are returned and objectives/translations are met
# Using the string in QuerySet._query as query